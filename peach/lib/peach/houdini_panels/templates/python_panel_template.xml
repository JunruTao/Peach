<?xml version="1.0" encoding="UTF-8"?>
<!--Build: [ ${STR_DATE_YMD} ] Peach
 [pHoudini - ${PEACH_VERSION}]
 This file contains definitions of houdini python panels.
 [ Houdini Notes ]:
 It should not be hand-edited when it is being used by the application.
 Note, that two definitions of the same element are not allowed in
 a single file. 
-->
<pythonPanelDocument>
    <interface 
        name="${PANEL_NAME}" 
        label="${PANEL_LABEL}" 
        icon="$PEACH_ICONS/icon_${PANEL_ICON}.svg" 
        showNetworkNavigationBar="true" 
        help_url="">
        <script>
            <![CDATA[${COMMENT_HEADER}${PANEL_SCRIPT}]]>
        </script>
        <includeInPaneTabMenu 
            menu_position="${PANEL_MENU_POS}" 
            create_separator="${PANEL_MENU_SPR}"/>
    <help><![CDATA[]]></help>
    </interface>
</pythonPanelDocument>
